/* ATRIAS3D_SHINROTATIONMATRICES_RIGHT 
 * Generated by writeOptimizedFunctions (10 Oct 2016 10:51:11)
 */

#include <mex.h>
#include "ATRIAS3D_ShinRotationMatrices_Right.h"

void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[])
{
	double * q ;
	double (*R1)[][3] ;
	double (*R5)[][3] ;

	/* Check for proper number of arguments. */
	if(nlhs!=2) mexErrMsgTxt("MEX function ATRIAS3D_ShinRotationMatrices_Right expects 2 output arguments.");
	if(nrhs!=1) mexErrMsgTxt("MEX function ATRIAS3D_ShinRotationMatrices_Right expects 1 input arguments.");

	/* Process Inputs */
	if(!(mxGetM(prhs[0])==13 && mxGetN(prhs[0])==1) && !(mxGetM(prhs[0])==1 && mxGetN(prhs[0])==13) )
		mexErrMsgTxt("Incorrect dimensions for input #1 - Should be a vector of length 13") ;
	q = mxGetPr(prhs[0]) ;


	/* Define Output variables */
	plhs[0] = mxCreateDoubleMatrix(3,3, mxREAL);
	R1 = (double (*)[][3]) mxGetPr(plhs[0]) ;
	plhs[1] = mxCreateDoubleMatrix(3,3, mxREAL);
	R5 = (double (*)[][3]) mxGetPr(plhs[1]) ;

	/* Invoke function */
	ATRIAS3D_ShinRotationMatrices_Right(q, *R1, *R5) ;
}
