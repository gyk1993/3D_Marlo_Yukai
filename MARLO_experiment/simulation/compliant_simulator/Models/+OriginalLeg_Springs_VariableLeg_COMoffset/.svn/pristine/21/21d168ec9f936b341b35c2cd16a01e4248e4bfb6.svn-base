/* ATRIAS3D_FOOTPOSITIONJACOBIANS_RIGHT 
 * Generated by writeOptimizedFunctions (10 Oct 2016 10:51:09)
 */

#include <mex.h>
#include "ATRIAS3D_FootPositionJacobians_Right.h"

void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[])
{
	double * q ;
	double * dq ;
	double (*J4R)[][3] ;
	double (*J4L)[][3] ;
	double *dJ4R ;
	double *dJ4L ;

	/* Check for proper number of arguments. */
	if(nlhs!=4) mexErrMsgTxt("MEX function ATRIAS3D_FootPositionJacobians_Right expects 4 output arguments.");
	if(nrhs!=2) mexErrMsgTxt("MEX function ATRIAS3D_FootPositionJacobians_Right expects 2 input arguments.");

	/* Process Inputs */
	if(!(mxGetM(prhs[0])==13 && mxGetN(prhs[0])==1) && !(mxGetM(prhs[0])==1 && mxGetN(prhs[0])==13) )
		mexErrMsgTxt("Incorrect dimensions for input #1 - Should be a vector of length 13") ;
	q = mxGetPr(prhs[0]) ;

	if(!(mxGetM(prhs[1])==13 && mxGetN(prhs[1])==1) && !(mxGetM(prhs[1])==1 && mxGetN(prhs[1])==13) )
		mexErrMsgTxt("Incorrect dimensions for input #2 - Should be a vector of length 13") ;
	dq = mxGetPr(prhs[1]) ;


	/* Define Output variables */
	plhs[0] = mxCreateDoubleMatrix(3,13, mxREAL);
	J4R = (double (*)[][3]) mxGetPr(plhs[0]) ;
	plhs[1] = mxCreateDoubleMatrix(3,13, mxREAL);
	J4L = (double (*)[][3]) mxGetPr(plhs[1]) ;
	plhs[2] = mxCreateDoubleMatrix(3,1, mxREAL);
	dJ4R = mxGetPr(plhs[2]) ;
	plhs[3] = mxCreateDoubleMatrix(3,1, mxREAL);
	dJ4L = mxGetPr(plhs[3]) ;

	/* Invoke function */
	ATRIAS3D_FootPositionJacobians_Right(q, dq, *J4R, *J4L, dJ4R, dJ4L) ;
}
