/* ATRIAS3D_FOOTYAWJACOBIANS_LEFT 
 * Generated by writeOptimizedFunctions (10 Oct 2016 11:14:58)
 */

#include <mex.h>
#include "ATRIAS3D_FootYawJacobians_Left.h"

void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[])
{
	double * q ;
	double * dq ;
	double *Jp4R_yaw ;
	double *Jp4L_yaw ;
	double *dJp4R_yaw ;
	double *dJp4L_yaw ;

	/* Check for proper number of arguments. */
	if(nlhs!=4) mexErrMsgTxt("MEX function ATRIAS3D_FootYawJacobians_Left expects 4 output arguments.");
	if(nrhs!=2) mexErrMsgTxt("MEX function ATRIAS3D_FootYawJacobians_Left expects 2 input arguments.");

	/* Process Inputs */
	if(!(mxGetM(prhs[0])==13 && mxGetN(prhs[0])==1) && !(mxGetM(prhs[0])==1 && mxGetN(prhs[0])==13) )
		mexErrMsgTxt("Incorrect dimensions for input #1 - Should be a vector of length 13") ;
	q = mxGetPr(prhs[0]) ;

	if(!(mxGetM(prhs[1])==13 && mxGetN(prhs[1])==1) && !(mxGetM(prhs[1])==1 && mxGetN(prhs[1])==13) )
		mexErrMsgTxt("Incorrect dimensions for input #2 - Should be a vector of length 13") ;
	dq = mxGetPr(prhs[1]) ;


	/* Define Output variables */
	plhs[0] = mxCreateDoubleMatrix(1,13, mxREAL);
	Jp4R_yaw = mxGetPr(plhs[0]) ;
	plhs[1] = mxCreateDoubleMatrix(1,13, mxREAL);
	Jp4L_yaw = mxGetPr(plhs[1]) ;
	plhs[2] = mxCreateDoubleMatrix(1,1, mxREAL);
	dJp4R_yaw = mxGetPr(plhs[2]) ;
	plhs[3] = mxCreateDoubleMatrix(1,1, mxREAL);
	dJp4L_yaw = mxGetPr(plhs[3]) ;

	/* Invoke function */
	ATRIAS3D_FootYawJacobians_Left(q, dq, Jp4R_yaw, Jp4L_yaw, dJp4R_yaw, dJp4L_yaw) ;
}
