/* ATRIAS3D_LAGRANGE_LEFT 
 * Generated by writeOptimizedFunctions (10 Oct 2016 11:20:48)
 */

#include <mex.h>
#include "ATRIAS3D_Lagrange_Left.h"

void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[])
{
	double * q ;
	double * dq ;
	double (*D_retval)[][13] ;
	double *Cdq ;
	double *G ;
	double (*B)[][13] ;

	/* Check for proper number of arguments. */
	if(nlhs!=4) mexErrMsgTxt("MEX function ATRIAS3D_Lagrange_Left expects 4 output arguments.");
	if(nrhs!=2) mexErrMsgTxt("MEX function ATRIAS3D_Lagrange_Left expects 2 input arguments.");

	/* Process Inputs */
	if(!(mxGetM(prhs[0])==13 && mxGetN(prhs[0])==1) && !(mxGetM(prhs[0])==1 && mxGetN(prhs[0])==13) )
		mexErrMsgTxt("Incorrect dimensions for input #1 - Should be a vector of length 13") ;
	q = mxGetPr(prhs[0]) ;

	if(!(mxGetM(prhs[1])==13 && mxGetN(prhs[1])==1) && !(mxGetM(prhs[1])==1 && mxGetN(prhs[1])==13) )
		mexErrMsgTxt("Incorrect dimensions for input #2 - Should be a vector of length 13") ;
	dq = mxGetPr(prhs[1]) ;


	/* Define Output variables */
	plhs[0] = mxCreateDoubleMatrix(13,13, mxREAL);
	D_retval = (double (*)[][13]) mxGetPr(plhs[0]) ;
	plhs[1] = mxCreateDoubleMatrix(13,1, mxREAL);
	Cdq = mxGetPr(plhs[1]) ;
	plhs[2] = mxCreateDoubleMatrix(13,1, mxREAL);
	G = mxGetPr(plhs[2]) ;
	plhs[3] = mxCreateDoubleMatrix(13,6, mxREAL);
	B = (double (*)[][13]) mxGetPr(plhs[3]) ;

	/* Invoke function */
	ATRIAS3D_Lagrange_Left(q, dq, *D_retval, Cdq, G, *B) ;
}
