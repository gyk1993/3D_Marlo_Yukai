/* ATRIAS3D_POSITIONSSPRINGCOM_HIP 
 * Generated by writeOptimizedFunctions (10 Oct 2016 10:24:45)
 */

#include <mex.h>
#include "ATRIAS3D_PositionsSpringCOM_Hip.h"

void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[])
{
	double * q ;
	double *pcm5R ;
	double *pcm6R ;
	double *pcm5L ;
	double *pcm6L ;

	/* Check for proper number of arguments. */
	if(nlhs!=4) mexErrMsgTxt("MEX function ATRIAS3D_PositionsSpringCOM_Hip expects 4 output arguments.");
	if(nrhs!=1) mexErrMsgTxt("MEX function ATRIAS3D_PositionsSpringCOM_Hip expects 1 input arguments.");

	/* Process Inputs */
	if(!(mxGetM(prhs[0])==16 && mxGetN(prhs[0])==1) && !(mxGetM(prhs[0])==1 && mxGetN(prhs[0])==16) )
		mexErrMsgTxt("Incorrect dimensions for input #1 - Should be a vector of length 16") ;
	q = mxGetPr(prhs[0]) ;


	/* Define Output variables */
	plhs[0] = mxCreateDoubleMatrix(3,1, mxREAL);
	pcm5R = mxGetPr(plhs[0]) ;
	plhs[1] = mxCreateDoubleMatrix(3,1, mxREAL);
	pcm6R = mxGetPr(plhs[1]) ;
	plhs[2] = mxCreateDoubleMatrix(3,1, mxREAL);
	pcm5L = mxGetPr(plhs[2]) ;
	plhs[3] = mxCreateDoubleMatrix(3,1, mxREAL);
	pcm6L = mxGetPr(plhs[3]) ;

	/* Invoke function */
	ATRIAS3D_PositionsSpringCOM_Hip(q, pcm5R, pcm6R, pcm5L, pcm6L) ;
}
