/* ATRIAS3D_FOOTPOINTPOSJACOB_HIP 
 * Generated by writeOptimizedFunctions (13 Oct 2016 17:39:44)
 */

#include <mex.h>
#include "ATRIAS3D_FootPointPosJacob_Hip.h"

void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[])
{
	double * q ;
	double * extra_vars ;
	double *p4R ;
	double (*R1)[][3] ;
	double (*J4R)[][3] ;
	double (*JR1Col)[][9] ;
	double *p4L ;
	double (*R5)[][3] ;
	double (*J4L)[][3] ;
	double (*JR5Col)[][9] ;

	/* Check for proper number of arguments. */
	if(nlhs!=8) mexErrMsgTxt("MEX function ATRIAS3D_FootPointPosJacob_Hip expects 8 output arguments.");
	if(nrhs!=2) mexErrMsgTxt("MEX function ATRIAS3D_FootPointPosJacob_Hip expects 2 input arguments.");

	/* Process Inputs */
	if(!(mxGetM(prhs[0])==16 && mxGetN(prhs[0])==1) && !(mxGetM(prhs[0])==1 && mxGetN(prhs[0])==16) )
		mexErrMsgTxt("Incorrect dimensions for input #1 - Should be a vector of length 16") ;
	q = mxGetPr(prhs[0]) ;

	if(!(mxGetM(prhs[1])==4 && mxGetN(prhs[1])==1) && !(mxGetM(prhs[1])==1 && mxGetN(prhs[1])==4) )
		mexErrMsgTxt("Incorrect dimensions for input #2 - Should be a vector of length 4") ;
	extra_vars = mxGetPr(prhs[1]) ;


	/* Define Output variables */
	plhs[0] = mxCreateDoubleMatrix(3,1, mxREAL);
	p4R = mxGetPr(plhs[0]) ;
	plhs[1] = mxCreateDoubleMatrix(3,3, mxREAL);
	R1 = (double (*)[][3]) mxGetPr(plhs[1]) ;
	plhs[2] = mxCreateDoubleMatrix(3,16, mxREAL);
	J4R = (double (*)[][3]) mxGetPr(plhs[2]) ;
	plhs[3] = mxCreateDoubleMatrix(9,16, mxREAL);
	JR1Col = (double (*)[][9]) mxGetPr(plhs[3]) ;
	plhs[4] = mxCreateDoubleMatrix(3,1, mxREAL);
	p4L = mxGetPr(plhs[4]) ;
	plhs[5] = mxCreateDoubleMatrix(3,3, mxREAL);
	R5 = (double (*)[][3]) mxGetPr(plhs[5]) ;
	plhs[6] = mxCreateDoubleMatrix(3,16, mxREAL);
	J4L = (double (*)[][3]) mxGetPr(plhs[6]) ;
	plhs[7] = mxCreateDoubleMatrix(9,16, mxREAL);
	JR5Col = (double (*)[][9]) mxGetPr(plhs[7]) ;

	/* Invoke function */
	ATRIAS3D_FootPointPosJacob_Hip(q, extra_vars, p4R, *R1, *J4R, *JR1Col, p4L, *R5, *J4L, *JR5Col) ;
}
